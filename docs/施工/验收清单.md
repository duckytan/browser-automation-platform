# ✅ 验收清单 · BAP 路径 X

> **规则**：每任务完成后跑验收命令 + 反向验证，输出必须真实（P43 证据铁律）。

---

## 任务 1 · 建 8 个核心目录骨架

```bash
# 验收
ls -d bin workflows engines hub operator connector humanizer recorder monitor state-tool
# 反向验证
test -d hub && test -d operator && echo "目录 OK"
```
**结果**：⏳ 待执行

---

## 任务 2 · 安装 just（公共路径）

```bash
cp /tmp/just /home/node/tools/bin/just
chmod +x /home/node/tools/bin/just
# 验收
/home/node/tools/bin/just --version   # 应输出 just 1.58.0
```
**结果**：⏳ 待执行（just 1.58.0 已下载 /tmp，未装）

---

## 任务 3 · 写 justfile 入口

```bash
# 验收
just --list   # 应显示 7 个 recipe（list/smoke/weibo-login/bilibili-up/wechat-article/slider-captcha/batch-fetch）
# 反向验证
just smoke    # 能跑（占位 echo 即可）
```
**结果**：⏳ 待执行

---

## 任务 4 · 填 5 个 .just 模板

```bash
# 验收
wc -l references/examples/*.just   # 每文件 >20 行
# 反向验证
just --list                        # 无报错
```
**结果**：⏳ 待执行（当前全空壳 6 行）

---

## 任务 5 · 写 install.sh（软链接）

```bash
# 验收
bash install.sh --dry-run   # 打印软链接计划不执行
# 反向验证
test -f install.sh && echo "install.sh OK"
```
**结果**：⏳ 待执行

---

## P1 完成判定（硬指标）

- [ ] 8 目录建成 + `just --version` = 1.58.0
- [ ] justfile 入口 + 5 模板骨架 + install.sh dry-run 通过
- [ ] PROGRESS.md 记录每步实测输出

---

_验收清单 v1.0 · 周星星 🌟 · 2026-08-10 · 待开工_
