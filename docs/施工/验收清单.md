# ✅ 验收清单 · BAP 路径 X

> **规则**：每任务完成后跑验收命令 + 反向验证，输出必须真实（P43 证据铁律）。

---

## 任务 1 · 建 8 个缺失目录骨架

```bash
# 工作目录：项目根（/home/node/projects/browser-automation-platform）
mkdir -p workflows engines hub operator humanizer recorder monitor state-tool
# 验收
ls -d workflows engines hub operator humanizer recorder monitor state-tool
# 反向验证
test -d hub && test -d operator && echo "目录 OK"
```
**结果**：✅ 完成 19:40（8 目录建成 · 目录 OK）

---

## 任务 2 · 安装 just（公共路径 + sha256 校验）

```bash
# 工作目录：项目根
sha256sum /tmp/just   # 与官方 release SHA256 比对（https://github.com/casey/just/releases/tag/1.58.0）
cp /tmp/just /home/node/tools/bin/just
chmod +x /home/node/tools/bin/just
# 验收
/home/node/tools/bin/just --version   # 应输出 just 1.58.0
```
**结果**：✅ 完成 19:40（sha256=3ad66571… · just 1.58.0 · which 命中 /home/node/tools/bin/just）

---

## 任务 3 · 写 justfile 入口（含 import 模板）

```bash
# 工作目录：项目根
# 验收
just --list   # 应显示全部 recipe（list/smoke + import 的 5 模板）
# 反向验证
just smoke    # 骨架阶段 echo 占位即可（真实冒烟待 P5）
```
**结果**：✅ 完成 19:40（just --list 显示 7 recipe · just smoke 可跑）

---

## 任务 4 · 填 5 个 .just 模板（骨架 · P2 填编排）

```bash
# 工作目录：项目根
# 验收
wc -l docs/references/examples/*.just   # 每文件 >20 行
just --justfile docs/references/examples/weibo-login.just --list   # 无报错
# 反向验证
just --list   # 根 justfile import 后能列出 5 模板，无报错
```
**结果**：✅ 完成 19:40（每文件 21-27 行 · import 后 7 recipe 全显示 · 变量唯一前缀避冲突）

---

## 任务 5 · 写 install.sh（软链接 · 仅 dry-run）

```bash
# 工作目录：项目根
# 验收
bash install.sh --dry-run   # 打印软链接计划不执行（6 skill: hub/connector/operator/humanizer/recorder/monitor）
# 反向验证
test -f install.sh && echo "install.sh OK"
```
**结果**：✅ 完成 19:40（dry-run 通过 · 6 skill 计划列出 · 未真软链）

---

## P1 完成判定（硬指标）

- [x] 8 目录建成 + `just --version` = 1.58.0 ✅
- [x] justfile 入口 + 5 模板骨架 + install.sh dry-run 通过 ✅
- [x] PROGRESS.md 记录每步实测输出 ✅

**P1 全部完成 2026-08-10 19:40**

---

_验收清单 v1.1 · 周星星 🌟 · 2026-08-10 · 待开工（已按三司会审 8 项修复）_
